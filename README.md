# Noon OCR

Arabic handwriting and print extraction playground.

## Run locally

```bash
npm install
```

Copy `.env.example` to `.env` and set `GEMINI_API_KEY` (optional second key: `backup_GEMINI_API_KEY`).

```bash
npm run dev
```

Open http://localhost:3000
